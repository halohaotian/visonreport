# VisonReport i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add bilingual support (English default + Chinese) with URL-prefix routing (`/en/...`, `/zh/...`) using Next.js 16's no-library i18n pattern.

**Architecture:** Proxy-based locale routing, JSON dictionary files loaded via dynamic imports, React context for client-side translations. All pages moved under `app/[locale]/`. API routes unchanged.

**Tech Stack:** Next.js 16 (proxy convention), React Context, JSON dictionaries, TypeScript

**Spec:** `docs/superpowers/specs/2026-04-15-i18n-design.md`

---

## File Map

### New files
- `dictionaries/en.json` — English translations
- `dictionaries/zh.json` — Chinese translations (extracted from current code)
- `dictionaries/index.ts` — getDictionary, type exports
- `src/proxy.ts` — locale detection/redirect (Next.js 16 proxy)
- `src/components/TranslationProvider.tsx` — React context for translations
- `src/components/LanguageSwitcher.tsx` — EN | 中 toggle

### Moved files
- `src/app/layout.tsx` → `src/app/[locale]/layout.tsx`
- `src/app/page.tsx` → `src/app/[locale]/page.tsx`
- `src/app/(auth)/login/page.tsx` → `src/app/[locale]/(auth)/login/page.tsx`
- `src/app/(auth)/register/page.tsx` → `src/app/[locale]/(auth)/register/page.tsx`
- `src/app/(auth)/pricing/page.tsx` → `src/app/[locale]/(auth)/pricing/page.tsx`
- `src/app/admin/page.tsx` → `src/app/[locale]/admin/page.tsx`
- `src/app/visualize/page.tsx` → `src/app/[locale]/visualize/page.tsx`

### Unchanged
- `src/app/api/*` — all API routes stay at root
- `src/lib/*` — all lib files unchanged
- `globals.css`, config files — unchanged

---

## Task 1: Create dictionary infrastructure

**Files:**
- Create: `dictionaries/zh.json`
- Create: `dictionaries/en.json`
- Create: `dictionaries/index.ts`
- Create: `src/components/TranslationProvider.tsx`

- [ ] **Step 1: Create `dictionaries/index.ts`**

```ts
import 'server-only';

const dictionaries = {
  en: () => import('./en.json').then((m) => m.default),
  zh: () => import('./zh.json').then((m) => m.default),
};

export type Locale = keyof typeof dictionaries;
export const locales = Object.keys(dictionaries) as Locale[];
export const defaultLocale: Locale = 'en';

export const hasLocale = (locale: string): locale is Locale =>
  locale in dictionaries;

export const getDictionary = async (locale: Locale) => dictionaries[locale]();
```

- [ ] **Step 2: Create `dictionaries/zh.json`** — extract all current Chinese strings

```json
{
  "nav": {
    "features": "功能",
    "demo": "效果预览",
    "pricing": "价格",
    "tryDemo": "上传体验",
    "login": "登录",
    "register": "注册"
  },
  "hero": {
    "badge": "AI 驱动 · 一键生成 · 即将上线",
    "title1": "需求文档太多太乱？",
    "title2": "一分钟看懂整个项目",
    "desc": "上传需求文档，AI 自动解析并生成项目需求全景可视化图。",
    "desc2": "让产品经理、开发、测试、业务方在同一张图上对齐理解。",
    "tryButton": "免费试用上传",
    "waitlistButton": "加入等候列表"
  },
  "painPoints": {
    "title": "这些痛苦，你一定经历过",
    "p1Title": "需求文档几十页，没人能看完",
    "p1Desc": "一个中型项目需求文档动辄几十页，团队成员根本看不完、记不住",
    "p2Title": "需求变更了，谁都不知道",
    "p2Desc": "改了哪个需求、影响哪些模块？没有全局视图，变更影响范围全靠猜",
    "p3Title": "各说各话，理解不一致",
    "p3Desc": "产品、开发、测试对同一个需求理解不同，返工率高达 30% 以上",
    "p4Title": "需求遗漏到上线才发现",
    "p4Desc": "非功能性需求、边界条件、异常流程经常遗漏，测试阶段才暴露"
  },
  "features": {
    "title": "核心功能",
    "f1Title": "AI 智能解析",
    "f1Desc": "上传 Word/Markdown/纯文本需求文档，AI 自动提取功能需求、非功能需求、约束条件",
    "f2Title": "需求全景图",
    "f2Desc": "一张图展示项目所有需求的模块、层级、关系。支持缩放、折叠、搜索",
    "f3Title": "遗漏检测",
    "f3Desc": "AI 自动检查需求覆盖度，识别可能遗漏的安全、性能、兼容性等维度",
    "f4Title": "多种视图",
    "f4Desc": "需求树、思维导图、模块分组……多种视图自由切换",
    "f5Title": "一键导出",
    "f5Desc": "导出为高清图片、PDF 或分享链接，发给团队和客户",
    "f6Title": "团队协作",
    "f6Desc": "多人实时在需求图上标注、评论、圈选（V2）"
  },
  "demo": {
    "title": "效果预览",
    "docLabel": "需求文档.docx",
    "mapLabel": "需求全景图 · 自动生成",
    "projectName": "电商平台 v2.0",
    "userMgmt": "用户管理",
    "orderMgmt": "订单管理",
    "productMgmt": "商品管理",
    "more": "... 还有 200 多条需求",
    "userReg": "用户注册",
    "userLogin": "用户登录",
    "permMgmt": "权限管理",
    "missPwdReset": "+ 遗漏: 密码重置",
    "createOrder": "创建订单",
    "payOrder": "订单支付",
    "refundOrder": "订单退款",
    "missTimeout": "+ 遗漏: 订单超时",
    "publishProduct": "商品发布",
    "inventory": "库存管理",
    "missSku": "+ 遗漏: SKU管理",
    "userPhone": "支持手机号、邮箱注册...",
    "userPwd": "支持密码、验证码、第三方...",
    "moduleUser": "一、用户管理模块",
    "moduleOrder": "二、订单管理模块"
  },
  "landingPricing": {
    "title": "简洁透明的价格",
    "singleName": "单次使用",
    "singlePrice": "9.9",
    "singleUnit": "次",
    "singleFeatures": ["上传1份文档", "生成全景图", "导出图片", "基础遗漏检测"],
    "singleCta": "按次购买",
    "personalName": "个人版",
    "personalPrice": "399",
    "personalUnit": "年",
    "personalFeatures": ["无限文档", "所有视图", "AI遗漏检测", "导出PDF", "分享链接", "早鸟¥99/年"],
    "personalCta": "立即订阅",
    "teamName": "团队版",
    "teamPrice": "1,899",
    "teamUnit": "年(5人)",
    "teamFeatures": ["个人版全部", "5人协作", "批注评论", "团队空间", "优先支持"],
    "teamCta": "团队订阅",
    "popular": "最受欢迎"
  },
  "waitlist": {
    "title": "加入等候列表，抢先体验",
    "desc": "早鸟价 ¥99/年（原价¥399/年），限前200名",
    "placeholder": "输入你的邮箱",
    "submitting": "提交中...",
    "submitButton": "抢先注册",
    "success": "注册成功！",
    "position": "你是第 {position} 位等候用户",
    "docSection": "一、用户管理模块",
    "docUserReg": "1.1 用户注册",
    "docUserRegDesc": "支持手机号、邮箱注册...",
    "docUserLogin": "1.2 用户登录",
    "docUserLoginDesc": "支持密码、验证码、第三方...",
    "docOrderSection": "二、订单管理模块",
    "docCreateOrder": "2.1 创建订单"
  },
  "footer": {
    "tagline": "需求全景可视化 — 让需求看得见，让项目看得清",
    "copyright": "© 2026 VisonReport"
  },
  "login": {
    "title": "登录你的账户",
    "email": "邮箱",
    "password": "密码",
    "passwordPlaceholder": "输入密码",
    "submitting": "登录中...",
    "submit": "登录",
    "noAccount": "还没有账户？",
    "registerLink": "立即注册",
    "loginFailed": "登录失败",
    "networkError": "网络错误"
  },
  "register": {
    "title": "创建你的账户",
    "name": "姓名（可选）",
    "namePlaceholder": "你的名字",
    "email": "邮箱",
    "password": "密码",
    "passwordPlaceholder": "至少6位",
    "submitting": "注册中...",
    "submit": "注册",
    "hasAccount": "已有账户？",
    "loginLink": "立即登录",
    "registerFailed": "注册失败",
    "networkError": "网络错误"
  },
  "pricing": {
    "title": "选择适合你的方案",
    "pleaseLogin": "请先",
    "loginLink": "登录",
    "hello": "你好，{email}",
    "processing": "处理中...",
    "subscribe": "订阅",
    "subscribeFailed": "订阅失败",
    "networkError": "网络错误",
    "single": { "name": "单次使用", "price": "¥9.9", "unit": "/次", "desc": "上传1份文档，生成全景图" },
    "personalMonthly": { "name": "个人版月付", "price": "¥49", "unit": "/月", "desc": "无限文档，所有视图" },
    "personalYearly": { "name": "个人版年付", "price": "¥399", "unit": "/年", "desc": "省¥189，含所有功能" },
    "teamMonthly": { "name": "团队版月付", "price": "¥199", "unit": "/月", "desc": "5人协作，批注评论" },
    "teamYearly": { "name": "团队版年付", "price": "¥1,899", "unit": "/年", "desc": "5人团队，最优性价比" },
    "enterprise": { "name": "企业版", "price": "¥999", "unit": "/月", "desc": "私有部署，无限人数" }
  },
  "admin": {
    "adminPanel": "管理后台",
    "backHome": "返回首页",
    "overview": "数据概览",
    "waitlistTab": "等候列表",
    "totalUsers": "总用户数",
    "totalWaitlist": "等候列表",
    "activeSubs": "活跃订阅",
    "totalRevenue": "总收入",
    "todayViews": "今日浏览",
    "todayClicks": "今日点击",
    "todayRegs": "今日注册",
    "todaySubs": "今日订阅",
    "dailyTrend": "每日趋势（近30天）",
    "date": "日期",
    "views": "浏览量",
    "clicks": "点击量",
    "regs": "注册数",
    "subs": "订阅数",
    "waitlistUsers": "等候列表用户",
    "email": "邮箱",
    "source": "来源",
    "regTime": "注册时间",
    "noWaitlist": "暂无等候列表用户",
    "loading": "加载中..."
  },
  "visualize": {
    "subtitle": "需求全景可视化",
    "title": "上传需求文档，生成全景图",
    "desc": "粘贴需求文本（Markdown 或纯文本格式），AI 自动解析并生成可视化",
    "placeholder": "示例：\n# 电商平台 v2.0\n\n## 一、用户管理\n1.1 用户注册 - 支持手机号、邮箱注册\n1.2 用户登录 - 支持密码、验证码、第三方登录\n1.3 权限管理 - 基于RBAC模型\n\n## 二、订单管理\n2.1 创建订单 - 支持单品和多品\n2.2 订单支付 - 支持微信、支付宝",
    "parsing": "AI 解析中...",
    "generate": "生成需求全景图",
    "enterText": "请输入需求文本",
    "parseFailed": "解析失败",
    "networkError": "网络错误",
    "reUpload": "重新上传",
    "modules": "个模块",
    "requirements": "条需求",
    "missing": "处可能遗漏",
    "missingTitle": "AI 检测到可能遗漏的需求",
    "totalCount": "共 {modules} 个模块，{requirements} 条需求，{missing} 处可能遗漏"
  }
}
```

- [ ] **Step 3: Create `dictionaries/en.json`** — English translations

```json
{
  "nav": {
    "features": "Features",
    "demo": "Demo",
    "pricing": "Pricing",
    "tryDemo": "Try Demo",
    "login": "Log In",
    "register": "Sign Up"
  },
  "hero": {
    "badge": "AI-Powered · One-Click · Coming Soon",
    "title1": "Drowning in requirements docs?",
    "title2": "See the whole project in 1 minute",
    "desc": "Upload requirement documents and let AI automatically parse and generate a visual architecture map.",
    "desc2": "Align product managers, developers, testers, and stakeholders on one single diagram.",
    "tryButton": "Try Free",
    "waitlistButton": "Join Waitlist"
  },
  "painPoints": {
    "title": "Sound familiar?",
    "p1Title": "Docs are dozens of pages — nobody reads them",
    "p1Desc": "A mid-sized project's requirements doc can be 50+ pages. Team members can't finish or remember them.",
    "p2Title": "Changes happen silently",
    "p2Desc": "Which requirement changed? What's affected? Without a global view, impact analysis is pure guesswork.",
    "p3Title": "Everyone understands differently",
    "p3Desc": "Product, dev, and QA interpret the same requirement differently. Rework rate exceeds 30%.",
    "p4Title": "Gaps found only after launch",
    "p4Desc": "Non-functional requirements, edge cases, and error flows slip through — caught only in testing."
  },
  "features": {
    "title": "Core Features",
    "f1Title": "AI-Powered Parsing",
    "f1Desc": "Upload Word, Markdown, or plain text. AI extracts functional, non-functional requirements and constraints.",
    "f2Title": "Requirement Panorama",
    "f2Desc": "One diagram shows all modules, hierarchies, and relationships. Zoom, collapse, and search supported.",
    "f3Title": "Gap Detection",
    "f3Desc": "AI checks requirement coverage and identifies missing security, performance, and compatibility aspects.",
    "f4Title": "Multiple Views",
    "f4Desc": "Requirement tree, mind map, module grouping — switch views freely.",
    "f5Title": "One-Click Export",
    "f5Desc": "Export as HD image, PDF, or shareable link for your team and clients.",
    "f6Title": "Team Collaboration",
    "f6Desc": "Real-time annotations, comments, and selections on the requirement map (V2)."
  },
  "demo": {
    "title": "See It In Action",
    "docLabel": "requirements.docx",
    "mapLabel": "Requirement Panorama · Auto-generated",
    "projectName": "E-Commerce v2.0",
    "userMgmt": "User Management",
    "orderMgmt": "Order Management",
    "productMgmt": "Product Management",
    "more": "... and 200+ more requirements",
    "userReg": "User Registration",
    "userLogin": "User Login",
    "permMgmt": "Permissions",
    "missPwdReset": "+ Missing: Password Reset",
    "createOrder": "Create Order",
    "payOrder": "Payment",
    "refundOrder": "Refund",
    "missTimeout": "+ Missing: Order Timeout",
    "publishProduct": "Product Listing",
    "inventory": "Inventory",
    "missSku": "+ Missing: SKU Mgmt",
    "userPhone": "Phone & email registration...",
    "userPwd": "Password, OTP, OAuth...",
    "moduleUser": "1. User Management Module",
    "moduleOrder": "2. Order Management Module"
  },
  "landingPricing": {
    "title": "Simple, Transparent Pricing",
    "singleName": "Pay Per Use",
    "singlePrice": "9.9",
    "singleUnit": "use",
    "singleFeatures": ["Upload 1 document", "Generate panorama", "Export image", "Basic gap detection"],
    "singleCta": "Buy Now",
    "personalName": "Personal",
    "personalPrice": "399",
    "personalUnit": "year",
    "personalFeatures": ["Unlimited docs", "All views", "AI gap detection", "PDF export", "Share links", "Early bird ¥99/yr"],
    "personalCta": "Subscribe",
    "teamName": "Team",
    "teamPrice": "1,899",
    "teamUnit": "year (5 users)",
    "teamFeatures": ["All Personal features", "5-user collaboration", "Annotations & comments", "Team workspace", "Priority support"],
    "teamCta": "Team Plan",
    "popular": "Most Popular"
  },
  "waitlist": {
    "title": "Join the Waitlist, Get Early Access",
    "desc": "Early bird price ¥99/yr (regular ¥399/yr), limited to first 200",
    "placeholder": "Enter your email",
    "submitting": "Submitting...",
    "submitButton": "Join Now",
    "success": "You're in!",
    "position": "You are #{position} on the waitlist",
    "docSection": "1. User Management Module",
    "docUserReg": "1.1 User Registration",
    "docUserRegDesc": "Phone & email registration...",
    "docUserLogin": "1.2 User Login",
    "docUserLoginDesc": "Password, OTP, OAuth...",
    "docOrderSection": "2. Order Management Module",
    "docCreateOrder": "2.1 Create Order"
  },
  "footer": {
    "tagline": "Visual Requirement Intelligence — See the big picture, ship with confidence",
    "copyright": "© 2026 VisonReport"
  },
  "login": {
    "title": "Welcome Back",
    "email": "Email",
    "password": "Password",
    "passwordPlaceholder": "Enter password",
    "submitting": "Logging in...",
    "submit": "Log In",
    "noAccount": "Don't have an account?",
    "registerLink": "Sign up",
    "loginFailed": "Login failed",
    "networkError": "Network error"
  },
  "register": {
    "title": "Create Your Account",
    "name": "Name (optional)",
    "namePlaceholder": "Your name",
    "email": "Email",
    "password": "Password",
    "passwordPlaceholder": "At least 6 characters",
    "submitting": "Signing up...",
    "submit": "Sign Up",
    "hasAccount": "Already have an account?",
    "loginLink": "Log in",
    "registerFailed": "Registration failed",
    "networkError": "Network error"
  },
  "pricing": {
    "title": "Choose Your Plan",
    "pleaseLogin": "Please",
    "loginLink": "log in",
    "hello": "Hello, {email}",
    "processing": "Processing...",
    "subscribe": "Subscribe",
    "subscribeFailed": "Subscription failed",
    "networkError": "Network error",
    "single": { "name": "Pay Per Use", "price": "¥9.9", "unit": "/use", "desc": "Upload 1 doc, generate panorama" },
    "personalMonthly": { "name": "Personal Monthly", "price": "¥49", "unit": "/mo", "desc": "Unlimited docs, all views" },
    "personalYearly": { "name": "Personal Yearly", "price": "¥399", "unit": "/yr", "desc": "Save ¥189, all features" },
    "teamMonthly": { "name": "Team Monthly", "price": "¥199", "unit": "/mo", "desc": "5-user collab, annotations" },
    "teamYearly": { "name": "Team Yearly", "price": "¥1,899", "unit": "/yr", "desc": "5-user team, best value" },
    "enterprise": { "name": "Enterprise", "price": "¥999", "unit": "/mo", "desc": "Self-hosted, unlimited users" }
  },
  "admin": {
    "adminPanel": "Admin Panel",
    "backHome": "Back to Home",
    "overview": "Overview",
    "waitlistTab": "Waitlist",
    "totalUsers": "Total Users",
    "totalWaitlist": "Waitlist",
    "activeSubs": "Active Subscriptions",
    "totalRevenue": "Total Revenue",
    "todayViews": "Today's Views",
    "todayClicks": "Today's Clicks",
    "todayRegs": "Today's Signups",
    "todaySubs": "Today's Subscriptions",
    "dailyTrend": "Daily Trend (Last 30 Days)",
    "date": "Date",
    "views": "Views",
    "clicks": "Clicks",
    "regs": "Signups",
    "subs": "Subscriptions",
    "waitlistUsers": "Waitlist Users",
    "email": "Email",
    "source": "Source",
    "regTime": "Registered",
    "noWaitlist": "No waitlist users yet",
    "loading": "Loading..."
  },
  "visualize": {
    "subtitle": "Visual Requirement Intelligence",
    "title": "Upload Requirements, Generate Panorama",
    "desc": "Paste requirement text (Markdown or plain text), AI parses and generates visualization",
    "placeholder": "Example:\n# E-Commerce v2.0\n\n## 1. User Management\n1.1 User Registration - Phone & email\n1.2 User Login - Password, OTP, OAuth\n1.3 Permissions - RBAC model\n\n## 2. Order Management\n2.1 Create Order - Single & multi-item\n2.2 Payment - WeChat Pay, Alipay",
    "parsing": "AI parsing...",
    "generate": "Generate Requirement Panorama",
    "enterText": "Please enter requirement text",
    "parseFailed": "Parse failed",
    "networkError": "Network error",
    "reUpload": "Re-upload",
    "modules": "modules",
    "requirements": "requirements",
    "missing": "potential gaps",
    "missingTitle": "AI detected potentially missing requirements",
    "totalCount": "{modules} modules, {requirements} requirements, {missing} potential gaps"
  }
}
```

- [ ] **Step 4: Create `src/components/TranslationProvider.tsx`**

```tsx
'use client';
import { createContext, useContext } from 'react';

type Dict = Record<string, Record<string, any>>;
const TranslationContext = createContext<Dict>({});

export function useTranslations(namespace: string) {
  const dict = useContext(TranslationContext);
  return dict[namespace] ?? {};
}

export function TranslationProvider({ dict, children }: { dict: Dict; children: React.ReactNode }) {
  return (
    <TranslationContext.Provider value={dict}>
      {children}
    </TranslationContext.Provider>
  );
}
```

- [ ] **Step 5: Verify build**

Run: `cd app && npx next build 2>&1 | tail -5`
Expected: Build succeeds (no imports of these files yet, just creation)

- [ ] **Step 6: Commit**

```bash
git add dictionaries/ src/components/TranslationProvider.tsx
git commit -m "feat(i18n): add dictionary files and TranslationProvider"
```

---

## Task 2: Create proxy and restructure layout

**Files:**
- Create: `src/proxy.ts`
- Create: `src/app/[locale]/layout.tsx`
- Delete: `src/app/layout.tsx` (replaced by `[locale]/layout.tsx`)

- [ ] **Step 1: Create `src/proxy.ts`**

```ts
import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';

const locales = ['en', 'zh'];
const defaultLocale = 'en';

export function proxy(request: NextRequest) {
  const { pathname } = request.nextUrl;
  const pathnameHasLocale = locales.some(
    (locale) => pathname.startsWith(`/${locale}/`) || pathname === `/${locale}`
  );
  if (pathnameHasLocale) return;
  request.nextUrl.pathname = `/${defaultLocale}${pathname}`;
  return NextResponse.redirect(request.nextUrl);
}

export const config = {
  matcher: ['/((?!api|_next/static|_next/image|favicon.ico).*)'],
};
```

- [ ] **Step 2: Create `src/app/[locale]/layout.tsx`**

```tsx
import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import { notFound } from 'next/navigation';
import { getDictionary, hasLocale } from '@/../../dictionaries';
import { TranslationProvider } from '@/components/TranslationProvider';
import '../globals.css';

const geistSans = Geist({ variable: '--font-geist-sans', subsets: ['latin'] });
const geistMono = Geist_Mono({ variable: '--font-geist-mono', subsets: ['latin'] });

export async function generateStaticParams() {
  return [{ locale: 'en' }, { locale: 'zh' }];
}

export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> {
  const { locale } = await params;
  const isEn = locale === 'en';
  return {
    title: isEn ? 'VisonReport — Visual Requirement Intelligence' : 'VisonReport — 需求全景可视化，一分钟看懂整个项目',
    description: isEn
      ? 'Transform requirements into visual architecture diagrams with AI. See the whole project in 1 minute.'
      : '上传需求文档，AI一键生成项目需求全景可视化图。让团队在一分钟内看到需求全貌。',
  };
}

export default async function LocaleLayout({
  children,
  params,
}: {
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
}) {
  const { locale } = await params;
  if (!hasLocale(locale)) notFound();
  const dict = await getDictionary(locale);
  return (
    <html lang={locale === 'zh' ? 'zh-CN' : 'en'} className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">
        <TranslationProvider dict={dict}>
          {children}
        </TranslationProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 3: Delete old `src/app/layout.tsx`**

```bash
rm src/app/layout.tsx
```

- [ ] **Step 4: Verify build**

Run: `cd app && npx next build 2>&1 | tail -10`
Expected: Build fails because pages still reference old paths. This is expected — pages will be moved in subsequent tasks.

- [ ] **Step 5: Commit**

```bash
git add src/proxy.ts src/app/[locale]/layout.tsx
git rm src/app/layout.tsx
git commit -m "feat(i18n): add proxy.ts and [locale] layout"
```

---

## Task 3: Create LanguageSwitcher component

**Files:**
- Create: `src/components/LanguageSwitcher.tsx`

- [ ] **Step 1: Create `src/components/LanguageSwitcher.tsx`**

```tsx
'use client';
import { usePathname } from 'next/navigation';

export function LanguageSwitcher({ locale }: { locale: string }) {
  const pathname = usePathname();
  const switchLocale = locale === 'en' ? 'zh' : 'en';
  const label = locale === 'en' ? '中文' : 'EN';
  const newPath = pathname.replace(`/${locale}`, `/${switchLocale}`);

  return (
    <a href={newPath} className="px-3 py-1 text-sm text-gray-500 hover:text-blue-600 border border-gray-200 rounded-md hover:border-blue-300 transition">
      {label}
    </a>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/LanguageSwitcher.tsx
git commit -m "feat(i18n): add LanguageSwitcher component"
```

---

## Task 4: Move and refactor landing page

**Files:**
- Move: `src/app/page.tsx` → `src/app/[locale]/page.tsx`
- Delete: `src/app/page.tsx`

This is the largest file (~159 strings). All hardcoded Chinese text replaced with `useTranslations()` calls. All navigation `href` prefixed with locale.

- [ ] **Step 1: Create `src/app/[locale]/page.tsx`**

The file should:
1. Import `useTranslations` from `@/components/TranslationProvider`
2. Accept `locale` param: add `export default function Home({ params }: { params: Promise<{ locale: string }> })`
3. Get `locale` via `const { locale } = use(params)` — note: since this is `'use client'`, use `React.use()`
4. Replace all Chinese strings with `t.key` lookups
5. Prefix all navigation href with `/${locale}`: `/login` → `/${locale}/login`, `/register` → `/${locale}/register`, `/visualize` → `/${locale}/visualize`
6. Keep `href="/"` and `href="#..."` as-is (anchors and logo link stay)
7. Add `<LanguageSwitcher locale={locale} />` to header
8. Update `path: '/'` in stats track to `path: window.location.pathname`
9. Keep all styling, layout, and logic identical

Key replacements in the component body:
- `const nav = useTranslations('nav')` → use `nav.features`, `nav.demo`, etc.
- `const hero = useTranslations('hero')` → use `hero.badge`, `hero.title1`, etc.
- `const pp = useTranslations('painPoints')` → use `pp.title`, `pp.p1Title`, etc.
- `const feat = useTranslations('features')` → use `feat.title`, `feat.f1Title`, etc.
- Same pattern for `demo`, `landingPricing`, `waitlist`, `footer`

For data arrays (pain points, features, pricing cards), convert to use translated keys:
```tsx
const painPoints = [pp.p1Title, pp.p2Title, pp.p3Title, pp.p4Title].map((title, i) => ({
  icon: ['📋','🔀','👥','🔍'][i],
  title,
  desc: [pp.p1Desc, pp.p2Desc, pp.p3Desc, pp.p4Desc][i],
}));
```

For the waitlist success position text, use string replacement:
```tsx
<p>{wl.position.replace('{position}', <span className="text-yellow-300 font-bold text-2xl">{result.position}</span>)}</p>
```
Split into: `{wl.position.split('{position}')[0]}<span>...</span>{wl.position.split('{position}')[1]}`

- [ ] **Step 2: Delete `src/app/page.tsx`**

```bash
rm src/app/page.tsx
```

- [ ] **Step 3: Verify build**

Run: `cd app && npx next build 2>&1 | tail -10`
Expected: Build may still fail due to other missing pages. That's OK.

- [ ] **Step 4: Commit**

```bash
git add src/app/[locale]/page.tsx
git rm src/app/page.tsx
git commit -m "feat(i18n): move landing page to [locale] with translations"
```

---

## Task 5: Move and refactor auth pages

**Files:**
- Move: `src/app/(auth)/login/page.tsx` → `src/app/[locale]/(auth)/login/page.tsx`
- Move: `src/app/(auth)/register/page.tsx` → `src/app/[locale]/(auth)/register/page.tsx`
- Move: `src/app/(auth)/pricing/page.tsx` → `src/app/[locale]/(auth)/pricing/page.tsx`
- Delete: `src/app/(auth)/` (old directory)

- [ ] **Step 1: Create `src/app/[locale]/(auth)/login/page.tsx`**

Key changes from current login:
- Import `useTranslations`, `React`
- `const { locale } = React.use(params)` for params
- `const t = useTranslations('login')`
- Replace all Chinese strings: `t.title`, `t.email`, `t.password`, etc.
- `href="/"` → `href="/${locale}"` (logo link)
- `href="/register"` → `href="/${locale}/register"`
- `window.location.href = '/admin'` → `window.location.href = '/${locale}/admin'`
- `window.location.href = '/'` → `window.location.href = '/${locale}'`
- Error messages: `data.error || t.loginFailed`, `t.networkError`

- [ ] **Step 2: Create `src/app/[locale]/(auth)/register/page.tsx`**

Same pattern as login:
- `const t = useTranslations('register')`
- All href and redirect updates with locale prefix
- Error messages use `t.registerFailed`, `t.networkError`

- [ ] **Step 3: Create `src/app/[locale]/(auth)/pricing/page.tsx`**

Key changes:
- `const t = useTranslations('pricing')`
- Plans array uses `t.single.name`, `t.single.price`, etc.
- `window.location.href = '/register'` → `window.location.href = '/${locale}/register'`
- `href="/login"` → `href="/${locale}/login"`

- [ ] **Step 4: Delete old auth directory**

```bash
rm -rf src/app/(auth)
```

- [ ] **Step 5: Commit**

```bash
git add src/app/[locale]/(auth)/
git rm -r src/app/(auth)/
git commit -m "feat(i18n): move auth pages to [locale] with translations"
```

---

## Task 6: Move and refactor admin page

**Files:**
- Move: `src/app/admin/page.tsx` → `src/app/[locale]/admin/page.tsx`
- Delete: `src/app/admin/` (old directory)

- [ ] **Step 1: Create `src/app/[locale]/admin/page.tsx`**

Key changes:
- `const t = useTranslations('admin')`
- All stat labels use `t.totalUsers`, `t.totalViews`, etc.
- Table headers use `t.date`, `t.views`, `t.clicks`, etc.
- `window.location.href = '/login'` → `window.location.href = '/${locale}/login'`
- `href="/"` → `href="/${locale}"`
- Loading text: `t.loading`
- Empty waitlist text: `t.noWaitlist`

- [ ] **Step 2: Delete old admin directory**

```bash
rm -rf src/app/admin
```

- [ ] **Step 3: Commit**

```bash
git add src/app/[locale]/admin/page.tsx
git rm -r src/app/admin/
git commit -m "feat(i18n): move admin page to [locale] with translations"
```

---

## Task 7: Move and refactor visualize page

**Files:**
- Move: `src/app/visualize/page.tsx` → `src/app/[locale]/visualize/page.tsx`
- Delete: `src/app/visualize/` (old directory)

- [ ] **Step 1: Create `src/app/[locale]/visualize/page.tsx`**

Key changes:
- `const t = useTranslations('visualize')`
- All labels use `t.title`, `t.desc`, `t.generate`, etc.
- `href="/"` → `href="/${locale}"`
- Error messages: `t.enterText`, `t.parseFailed`, `t.networkError`
- Stats text: build from `t.modules`, `t.requirements`, `t.missing`
- Placeholder: `t.placeholder`

- [ ] **Step 2: Delete old visualize directory**

```bash
rm -rf src/app/visualize
```

- [ ] **Step 3: Commit**

```bash
git add src/app/[locale]/visualize/page.tsx
git rm -r src/app/visualize/
git commit -m "feat(i18n): move visualize page to [locale] with translations"
```

---

## Task 8: Final verification and deploy

- [ ] **Step 1: Run full build**

Run: `cd app && npx next build 2>&1`
Expected: Build succeeds with all routes under `[locale]`

- [ ] **Step 2: Verify route structure**

Check build output shows:
```
├ ○ /en
├ ○ /zh
├ ○ /en/login
├ ○ /zh/login
... etc
```

- [ ] **Step 3: Fix any build errors**

Common issues:
- TypeScript errors from `React.use()` — may need `import React from 'react'`
- Missing `params` typing — use `Promise<{ locale: string }>` pattern
- CSS import path may need adjustment in layout

- [ ] **Step 4: Deploy to Vercel**

```bash
cd app && vercel --prod
```

- [ ] **Step 5: Test live**

- Visit `/` → should redirect to `/en`
- Visit `/en` → English landing page
- Visit `/zh` → Chinese landing page
- Click language switcher → toggles between EN/ZH
- Test `/en/login`, `/zh/login` → correct language
- Test `/en/admin` → admin dashboard in English

- [ ] **Step 6: Final commit**

```bash
git add -A
git commit -m "feat(i18n): complete bilingual support with URL-prefix routing"
```
